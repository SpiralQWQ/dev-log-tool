## 📅 程序员日志归档工作流 V2.2

> **JIT 路由表说明**：当使用 `CLAUDE.template.md` 架构时，本文件通过 JIT 路由表自动注册。当用户说「日志/归档/程序员日志」时，路由表触发前置读取此文件。若你的项目不使用 JIT 路由表，将 `CLAUDE_snippet.md` 追加到 `CLAUDE.md` 即可。
>
> 量化数据采集脚本见同仓库 `collect/` 目录。

### 触发词

`程序员日志` `记录今天日志` `日志归档` `dev-log`

### 流程

1. **防误触握手** → 检测触发词 → 询问确认 → 等待用户回复「确认」
2. **自动采集**（每次确认后都重新跑，确保数据最新）：
   - **自动运行 `collect/collect_daily_data.ps1`**（约 10 秒）
   - 读取生成的 JSON（路径：`<日志根目录>\Josn\daily_data_YYYY-MM-DD.json`）
   - JSON 字段：AW 时间分类 / onefetch 仓库快照 / git 提交统计 / 终端历史
   - 同天旧 JSON 自动备份到 `<日志根目录>\Josn\Prev\daily_data_prev_YYYY-MM-DD_HHmm.json`
3. **数据提取** → 本对话框核心代码变更 + 关键 Prompt + 最终方案（过滤报错/重试）
4. **路径锁定** → 目标：`<YOUR_LOG_DIR>`（替换为你的实际日志归档路径）
5. **检查同天日志** → 扫描 `<YOUR_LOG_DIR>` 下是否有今天（`YYYYMMDD`）的日志文件
   - 有 → **读取现有日志内容，按第 7 步合并**
   - 无 → **新建文件，走序号分配**
6. **序号命名** → `NN_关于<概要>的日志_<工作区>_YYYYMMDD_HHmm.md`（HHmm 必填，24小时制）
   - 新建时：扫描目录最大序号 + 1
   - 合并时：保留原始文件名，不重新编号
7. **结构化输出** → 固定 8 字段 Markdown 模板（见下方）
   - **新建**：直接写入完整 8 字段头 + 正文
   - **合并**：在现有日志末尾附加 `## 时段N：标题（HH:MM–HH:MM）` 分区，不重复 8 字段头

### 模板（完整态 — 量化数据完整）

```markdown
# NN_关于XXX的日志

> **日期**: YYYY-MM-DD（时段）
> **会话起止**: 约 HH:MM → HH:MM UTC+8
> **模型**: [模型名，未知填"未记录"]
> **Token 消耗**: [估算，未知填"未记录"]
> **花费反馈**: [如有/未记录]
> **变更记录**: [核心变更列表；分号分隔] | 编码 3h12min / 浏览器 1h05min / 终端 45min | +3/-12/-1
> **文件状态**: [文件变更统计，+ 新增 / ~ 修改 / - 删除]
> **量化快照**: TypeScript 56.9% · 28,450 行 · 3465 文件 · 1 作者
```

### 模板（部分态 — 量化数据部分可用）

```markdown
# NN_关于XXX的日志

> **日期**: YYYY-MM-DD（时段）
> **会话起止**: 约 HH:MM → HH:MM UTC+8
> **模型**: [模型名，未知填"未记录"]
> **Token 消耗**: [估算，未知填"未记录"]
> **花费反馈**: [如有/未记录]
> **变更记录**: [核心变更列表；分号分隔] | 编码 3h12min / 浏览器 1h05min / 终端 45min
> **文件状态**: [文件变更统计，+ 新增 / ~ 修改 / - 删除]
> **量化快照**: (无/未采集到)
```

### 模板（传统态 — 量化数据不存在）

```markdown
# NN_关于XXX的日志

> **日期**: YYYY-MM-DD（时段）
> **会话起止**: 约 HH:MM → HH:MM UTC+8
> **模型**: [模型名，未知填"未记录"]
> **Token 消耗**: [估算，未知填"未记录"]
> **花费反馈**: [如有/未记录]
> **变更记录**: [核心变更列表；分号分隔]
> **文件状态**: [文件变更统计，+ 新增 / ~ 修改 / - 删除]
> **量化快照**: (未启用数据采集)
```

8 个字段缺一不可，顺序固定。未知数据填「未记录」，禁止留空。

### 量化数据融合规则（V2.0+）

| 数据源 | 日志字段 | 说明 |
|--------|---------|------|
| ActivityWatch | 变更记录尾部 | 编码/浏览器/终端等时间分类（仅非零类别显示） |
| onefetch | 量化快照 | 仓库语言 · 行数 · 文件数 · 作者数 |
| git log | 变更记录尾部 | `+新增/-删除/-修改` 文件变更统计 |
| PSReadLine | 正文可选引用 | 终端历史采样（已脱敏） |

### 配置一致性检查

如果日志流程出现异常（数据找不到、路径不匹配、字段缺失），运行以下检查脚本诊断：

```powershell
# 替换 <YOUR_COLLECT_DIR> 为你的 collect/ 实际路径
Test-Path "<YOUR_COLLECT_DIR>\activity_categories.json"
Test-Path "<YOUR_COLLECT_DIR>\sensitive_patterns.json"
```

### 约束

- 同一天多条日志必须合并为一条，内部用 `## 时段N：标题（HH:MM–HH:MM）` 分区展示
- 对话复盘用 `<details><summary>` 折叠
- 提取不代表删除——仅复制/归档，不修改源文件
- **密钥遮蔽铁律**：日志中严禁出现任何 API Key、Token、密码明文。出现位置必须用 `***` 打码
